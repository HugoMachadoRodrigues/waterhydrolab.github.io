{%- comment -%}
  whl_news.liquid — lab news as blocks (a small blog). Each _news item has a
  date, an optional image (uploaded in the CMS), an optional headline, and body
  text. (We use `headline`, not `title`: Jekyll auto-fills an untitled post's
  `title` from its filename, which would show the random CMS slug.)
  Newest first. Pass scroll=true on the homepage to wrap the items in a
  fixed-height, scrollable panel; on /news/ (no scroll) every item is listed.
  Usage:  {% include whl_news.liquid scroll=true %}
{%- endcomment -%}
{%- if site.news and site.news != empty -%}
  {%- assign news = site.news | sort: 'date' | reverse -%}
  {%- assign extra = '' -%}
  {%- if include.scroll -%}{%- assign extra = ' whl-news-scroll' -%}{%- endif -%}
  <div class="whl-news{{ extra }}">
    {%- for item in news -%}
      <article class="whl-news-item{% unless item.image and item.image != "" %} whl-news-noimg{% endunless %}">
        {%- if item.image and item.image != '' -%}
          <div class="whl-news-thumb">
            <img src="{{ item.image | relative_url }}" alt="{{ item.headline | default: 'Lab news' | escape }}" loading="lazy">
          </div>
        {%- endif -%}
        <div class="whl-news-body">
          <time class="whl-news-date">{{ item.date | date: '%b %d, %Y' }}</time>
          {%- if item.headline and item.headline != '' -%}
            <div class="whl-news-title">{{ item.headline }}</div>
          {%- endif -%}
          <div class="whl-news-text">{{ item.content }}</div>
        </div>
      </article>
    {%- endfor -%}
  </div>
{%- else -%}
  <p>No news yet.</p>
{%- endif -%}
